/*
 WARNING: Server-side only!
 
 The code in this tutorial requires the use of the primary key
 and should ONLY run in a trusted server-side environment.
 
 Never include your primary key in client applications!
 */

// This Swift code must run in a trusted server-side environment (for example, Parse-Server-Swift/Vapor),
// not in your client app. Parse Cloud Code itself is implemented in JavaScript (e.g., main.js) and would
// use a different, JavaScript-based implementation.
